{% extends 'base.html.twig' %}

{% block title %}OperationType index{% endblock %}

{% block body %}
    <h1>OperationType index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for operation_type in operation_types %}
            <tr>
                <td>{{ operation_type.id }}</td>
                <td>{{ operation_type.name }}</td>
                <td>
                    <a href="{{ path('operation_type_show', {'id': operation_type.id}) }}">show</a>
                    <a href="{{ path('operation_type_edit', {'id': operation_type.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('operation_type_new') }}">Create new</a>
{% endblock %}
