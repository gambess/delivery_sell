{% extends 'base.html.twig' %}

{% block title %}OperationType{% endblock %}

{% block body %}
    <h1>OperationType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ operation_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ operation_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('operation_type_index') }}">back to list</a>

    <a href="{{ path('operation_type_edit', {'id': operation_type.id}) }}">edit</a>

    {{ include('operation_type/_delete_form.html.twig') }}
{% endblock %}
