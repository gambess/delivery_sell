{% extends 'base.html.twig' %}

{% block title %}PaymentType{% endblock %}

{% block body %}
    <h1>PaymentType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ payment_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ payment_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('payment_type_index') }}">back to list</a>

    <a href="{{ path('payment_type_edit', {'id': payment_type.id}) }}">edit</a>

    {{ include('payment_type/_delete_form.html.twig') }}
{% endblock %}
