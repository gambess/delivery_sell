{% extends 'base.html.twig' %}

{% block title %}Paymethod{% endblock %}

{% block body %}
    <h1>Paymethod</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ paymethod.id }}</td>
            </tr>
            <tr>
                <th>ShortName</th>
                <td>{{ paymethod.shortName }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ paymethod.name }}</td>
            </tr>
            <tr>
                <th>IsActive</th>
                <td>{{ paymethod.isActive ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('paymethod_index') }}">back to list</a>

    <a href="{{ path('paymethod_edit', {'id': paymethod.id}) }}">edit</a>

    {{ include('paymethod/_delete_form.html.twig') }}
{% endblock %}
